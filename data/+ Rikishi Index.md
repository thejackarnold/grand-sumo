# Rikishi Index

This list is automatically generated from all files tagged with `#rikishi`.

```dataview
TABLE without id
  file.link as "Rikishi",
  rank,
  wins,
  losses,
  win_percentage,
  length(file.inlinks) AS "Mentions"
FROM #rikishi  
SORT file.name ASC
```
